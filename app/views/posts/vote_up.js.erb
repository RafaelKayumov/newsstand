$("#<%= @post.id%>.rating-value").html("<%= @post.votes.count %>");

/*
$("#<%= @post.id%>.rating-button").click(function() {
  $.ajax("/posts/<%= @post.id%>/vote_up")
  $("#<%= @post.id%>.rating-value").html('<%= @post.votes.count %>')
});
*/
