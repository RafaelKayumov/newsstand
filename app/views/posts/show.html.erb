<%= render @post %>

<% if @post.can_moderate(current_user) %>
  <p>
    <%= link_to 'Edit', edit_post_path(@post) %>, 
    <%= link_to 'Delete', post_path(@post),
      method: :delete,
      data: { confirm: 'Are you sure?' } 
    %>
  </p>
<% end %>
<p><%= link_to 'Back', posts_path %></p>
